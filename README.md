# React + TypeScript + Vite

`npm run dev`

Laboratory tasks:

create app burger constructor.
1 - create project,add css to burger from task
2 - create 2 containers, 1 - ingredients list, 2 - visual burger with count price
    2.1 on ingredient list every item have image, name, count, delete button.
        on click image(possibly on name) count +1,on delete count -1.
    2.2 on burger get container with div:   on top(div top-bun),
                                            on middle (div with added ingredients).
                                            on bottom(div bottom-bun).
        on bottom container get burger price.
                                          
3 - create interface{ name,price,img }